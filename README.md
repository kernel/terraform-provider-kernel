# Terraform Provider Kernel

Minimal Terraform Plugin Framework provider scaffold for Kernel.

This repository currently contains the provider binary entrypoint and an empty provider skeleton with Terraform type name `kernel`. API client behavior, authentication, resources, and data sources are intentionally left for later PRs.

## Development

Run the test suite:

```sh
go test ./...
```

